# Day 27：框架阶段综合复盘：完整链路与定位

> 今日目标：能不看笔记解释字典列表与新增流程，并用真实证据完成 15 题口头自测；薄弱处回补而不是继续堆新知识。

## 知识点速记（完成当天任务后复习）

一次标准数据流：Vue 页面 → API 封装 → `/dev-api` 代理 → Controller → Service → Mapper/XML → MySQL → JSON 返回。横切防线：Token/权限、Redis、日志、异常、事务。定位必须以 Network、日志、断点、DBeaver 证据为准。

## 视频、文档与若依对应点

- D17–D26 的笔记、截图、`SysDictType` 链路文件。
- 运行中的本机若依前后端；仅使用测试/临时数据。

## 本日重点、易混点与若依对应

复盘的目标是可追踪：每个节点有输入、输出和下一跳。不会某个点不代表失败；必须准确标为“待回补”，再回到对应 Day 的证据，而不是让 AI 生成大量新代码。

## 今天照做（09:00–23:00）

### 09:00–12:00｜字典列表 Debug

1. 后端用 Debug 启动；在 Controller list、Service 查询、Mapper 调用处各打一个断点。
2. 前端打开字典页，输入一个条件点击查询。按 F8 单步，必要时 F7 进入，记录 7 节点：页面事件、API、Network、Controller、Service、Mapper/XML、MySQL/响应。
3. 在 DBeaver 用等价 SELECT 对照 `rows/total`。记录一个参数值的变化。

### 15:00–18:00｜权限与新增链路

1. 以管理员和最小权限测试账号各访问一次，记录菜单/按钮差异。
2. 用临时字典走新增→重复失败→删除；对应 Controller、Service 唯一校验、操作日志、数据库结果。
3. 写“谁有权限、后端在哪拒绝、证据是什么”。

### 19:00–23:00｜15 题口头自测

1. 逐题录音或写简答：配置、代理、Controller、Service、Mapper、XML、事务、Token、权限、Redis、日志、异常、生成器、Network、DBeaver。
2. 每题标 A（能举项目例子）/B（能定义）/C（不会）。C 题回到 D19–D26 的对应文档，补一条证据。
3. 通过标准：能完整解释字典列表和新增；否则重复薄弱日，不进入前端学习。

## 今日完成清单

- [ ] 列表流程已 Debug，新增流程已验证。
- [ ] 有 15 题自测和 A/B/C 结果。
- [ ] 至少回补一个 C 项或确认全部通过。

---

## 任务参考答案（完成后再查看）

答案：发生 500 时为何还要查 DBeaver？它能验证 SQL/数据假设，但第一证据仍是后端异常日志与调用栈，不能跳过。

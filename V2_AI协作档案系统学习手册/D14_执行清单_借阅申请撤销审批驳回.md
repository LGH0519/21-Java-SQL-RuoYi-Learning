# D14 今天照做执行清单：申请、撤销、审批、驳回

## 上午：冻结规则（09:00–12:00）

1. [ ] 打开 D08 状态图；在纸上列：提交/撤销/批准/驳回的角色、旧状态、新状态、失败条件。
2. [ ] 在 DBeaver 确认 `archive_borrow_application` 表存在；不要手工修改状态数据。
3. [ ] 使用生成器导入申请表，按 D11/D12 的“导入→编辑→截图→diff”流程生成基础 CRUD。
4. [ ] 向 AI 输入状态表，要求只输出 Service 方法名、文件清单、测试矩阵；不生成代码。

## 下午：生成并联调（15:00–18:00）

1. [ ] 审查 AI 的文件清单后允许最小改动；检查没有修改 Security 配置或无关模块。
2. [ ] 用普通员工账号登录，进入申请页面，选择可借测试档案，点击 **提交**。
3. [ ] Network 记录请求和响应；DBeaver 查申请人、状态、创建时间。
4. [ ] 在待审批状态点击 **撤销**；确认状态变更为 CANCELED。
5. [ ] 重新提交后，用管理员账号登录，分别点击 **批准** 和另一条的 **驳回**；填写虚构驳回理由。

## 晚上：越权与状态测试（19:00–23:00）

1. [ ] 测试普通员工点审批、撤销已批准、重复提交、审批不存在 ID；记录每次失败响应。
2. [ ] 在 Service 状态判断行打断点，观察当前用户、旧状态、目标状态。
3. [ ] F12/Apifox/DBeaver 三处各保留一条成功和一条失败证据。

## 收尾验收

- [ ] 四个状态操作均按角色正常工作。
- [ ] 至少 4 类非法操作被后端拒绝。
- [ ] 数据库审批人/状态与页面一致。

# D11 今天照做执行清单：若依生成器准备

## 上午：确认生成器输入（09:00–12:00）

1. [ ] 浏览器登录若依，左侧进入 **系统工具 → 代码生成**；若菜单名不同，搜索“代码生成”。
2. [ ] 只查看页面的 **导入表**、**编辑表**、**生成代码** 按钮；今天先不要点击生成。
3. [ ] 打开 D10 `archive_v1.sql` 与字段字典；逐项检查表注释、列注释、主键、日期字段、状态字段。
4. [ ] 新建 `docs/design/generator-mapping.md`，为分类表填写：表名、模块名、业务名、类名、菜单名、权限前缀、查询列、表单列。
5. [ ] 对档案表重复；将状态字段标明要使用的字典类型。

## 下午：导入前核对（15:00–18:00）

1. [ ] 在 Git Bash 项目根目录输入 `git status`；若出现不理解的修改，停止并截图记录。
2. [ ] DBeaver 运行 `SELECT DATABASE();`，确认生成器要连接的项目数据库已包含目标表。
3. [ ] 在生成器点击 **导入表**；仅勾选 `archive_category`，先点击 **下一步/确定**前核对名称。
4. [ ] 导入后点击该表行右侧 **编辑**；对照 mapping 填/检查包路径、模块、业务、权限前缀、列属性。
5. [ ] 点击 **保存**，返回列表；对档案表重复。

## 晚上：生成前审查（19:00–23:00）

1. [ ] 不点击生成，先截图两张表的生成信息。
2. [ ] 向 AI 发送 mapping（不发送配置密码），要求输出“生成后预期前端/后端文件类别、生成器不能做的规则、测试列表”。
3. [ ] 在笔记勾选：编号唯一/借阅中删除/状态流转/事务均被标为“生成器不能自动完成”。
4. [ ] 写明明天生成顺序：先分类，再档案；每次生成后先看 Git diff。

## 收尾验收

- [ ] 两张表已导入并保存正确生成信息。
- [ ] 有 generator mapping 和生成前截图。
- [ ] Git 工作区没有未知改动。

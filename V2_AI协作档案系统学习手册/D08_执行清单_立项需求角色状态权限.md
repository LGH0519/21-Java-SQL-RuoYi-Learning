# D08 今天照做执行清单：立项、需求、角色、状态、权限

## 上午：需求文档（09:00–12:00）

1. [ ] 在项目仓库根目录创建/打开 `docs/design`；若目录不存在，右键根目录 → **New → Directory** → 输入 `docs`，再在其内建 `design`。
2. [ ] 新建 `requirements.md`；输入一级标题 `# 档案管理系统第一版需求`。
3. [ ] 复制 D08 文档的 AI 提示词，发送给 AI；要求 Markdown 草案，不生成代码。
4. [ ] 将草案粘贴入 `requirements.md`；人工补全“做什么/不做什么/验收标准”。
5. [ ] 检查没有多级审批、全文检索、跨部门调阅等范围外功能。

## 下午：角色和状态图（15:00–18:00）

1. [ ] 在 `requirements.md` 下新增 `## 角色矩阵`；建立角色×功能表。
2. [ ] 新建 `state-machines.md`；粘贴申请和借阅的 Mermaid 状态图草案。
3. [ ] 逐条检查每条箭头：谁点按钮、旧状态、新状态、失败条件、影响表。
4. [ ] 新建 `permissions.md`；输入权限编码，如 `archive:record:add`、`archive:borrow:approve`，并写对应角色。

## 晚上：测试前置（19:00–23:00）

1. [ ] 新建 `test-cases-draft.md`；每个状态动作写一条成功和一条失败用例。
2. [ ] 让 AI 只审状态跳转漏洞；将建议标“采纳/拒绝”。
3. [ ] 使用 Git Bash 进入项目根目录，运行 `git status`；确认只出现你创建的 docs，未意外改配置。
4. [ ] 若确认无误，运行 `git add docs/design`，再运行 `git status`；今天可不 commit，先截图 staged 内容。

## 收尾验收

- [ ] 需求、角色、状态、权限、测试草案 5 份资产存在。
- [ ] 每个操作有明确前后状态。
- [ ] Git 只包含预期文档修改。

# D16 今天照做执行清单：联调、测试、README、本地部署

## 上午：测试表（09:00–12:00）

1. [ ] 新建 `docs/test/test-cases.md`；建立列：编号、前置、操作、预期页面/接口、预期数据库、实际、证据。
2. [ ] 填满至少 15 行；必须含 5 类失败：权限不足、重复编号、状态非法、附件异常、事务失败。
3. [ ] 每执行一条，在“实际/证据”填 URL 截图名、DBeaver 查询截图名或日志行号。

## 下午：完整演示（15:00–18:00）

1. [ ] 从普通员工登录开始：检索档案 → 提交申请。
2. [ ] 切换管理员：审批 → 借出 → 归还。
3. [ ] 每一步查看 Network 状态码、页面状态、DBeaver 三表数据。
4. [ ] 执行 5 条失败用例；遇到问题按“Network→Controller→Service→Mapper/XML→数据库”记录，不直接让 AI 重写。

## 晚上：README、构建、Git（19:00–23:00）

1. [ ] 新建/完善根目录 `README.md`；固定标题：背景、角色与功能、技术栈、ER 图、启动前提、后端启动、前端启动、测试、限制、后续计划。
2. [ ] 让 AI 只生成 README 目录和草稿；人工填写真实且脱敏的启动步骤。
3. [ ] 终端在 backend 目录执行项目已有 Maven 打包命令；在 frontend 目录执行 `npm run build:prod`。不要执行升级/清理命令。
4. [ ] 若失败，保存第一条错误；若成功，记录生成位置。不要提交构建产物。
5. [ ] 运行 `git status`；检查没有 `application*.yml`、真实附件、token、`node_modules`、`target`。
6. [ ] 运行 `git add <明确的文件或目录>`；再运行 `git diff --staged` 人工审查后才 `git commit -m "feat: 完成档案管理系统第一版核心流程"`。

## 收尾验收

- [ ] 15 条测试和 5 条失败案例均有证据。
- [ ] README 可指导他人本地启动。
- [ ] 构建有成功证据或完整失败记录。
- [ ] Git 暂存区不含敏感信息和构建产物。

# D15 今天照做执行清单：借出、归还、事务、逾期

## 上午：事务设计确认（09:00–12:00）

1. [ ] 打开申请/借阅状态图；写“借出三步”：校验批准申请、插入借阅记录、档案改借阅中。
2. [ ] 写“归还三步”：校验借阅中、填实际归还时间、档案恢复可借。
3. [ ] 让 AI 输出 `@Transactional` Service 设计、抛异常点和回滚断言；不生成代码。
4. [ ] 人工检查事务在 Service，而不是 Controller；批准与实际借出不能混淆。

## 下午：生成与成功联调（15:00–18:00）

1. [ ] 接受 AI 最小代码后运行 `git diff --stat`，确认涉及申请/借阅/档案业务文件。
2. [ ] 创建一条已批准申请；管理员点击 **确认借出**。
3. [ ] DBeaver 同时查询申请、借阅记录、档案三表，截图三项状态。
4. [ ] 点击 **确认归还**，填测试日期；再次查三表，确认借阅记录已归还、档案可借。

## 晚上：失败回滚与逾期（19:00–23:00）

1. [ ] 用不存在申请 ID 或不合法状态调用借出接口；记录接口错误。
2. [ ] DBeaver 查三表，确认未生成半条借阅记录、档案状态未错误变化。
3. [ ] 在学习/测试记录中设置一个过去的应还日、实际归还为空；执行逾期 SELECT。
4. [ ] 在事务 Service 入口打断点，记录参数、状态变更前后、异常分支。

## 收尾验收

- [ ] 成功借出、归还、失败回滚都有三表证据。
- [ ] 能执行并解释逾期查询。
- [ ] 没有为实现功能改全局事务/依赖配置。

# D12 今天照做执行清单：生成分类与档案 CRUD

## 上午：生成分类模块（09:00–12:00）

1. [ ] Git Bash 进入项目根目录，运行 `git status`，截图当前状态。
2. [ ] 若依代码生成页面选中 `archive_category` → 点击 **生成代码**。
3. [ ] 选择/确认下载或生成方式；只生成该表，不能勾选无关表。
4. [ ] 将生成文件按若依说明放入对应前后端目录；执行前先列出目标路径，确认没有覆盖自定义文件。
5. [ ] 回 Git Bash 运行 `git status` 和 `git diff --stat`；确认只有分类模块预期文件。

## 下午：启动并验证分类 CRUD（15:00–18:00）

1. [ ] IDEA Maven 面板点击根项目 **Reload All Maven Projects**；不要升级版本。
2. [ ] Run 后端，前端终端运行 `npm run dev`。
3. [ ] 登录后在菜单找到分类模块；依次点击 **新增**、输入虚构分类名称、点击 **确定**。
4. [ ] F12 Network 找新增请求，记录 URL/状态码；DBeaver 查表确认新增行。
5. [ ] 再完成查询、修改、删除；每步都在页面、Network、数据库三处核对。

## 晚上：档案模块与规则（19:00–23:00）

1. [ ] 对 `archive_record` 重复“生成 → Git diff → 启动 → CRUD”流程。
2. [ ] 给 AI 输入唯一编号和借阅中不能删除的规则，要求先输出文件清单/Service 规则/四个测试。
3. [ ] 审查后才接受最小代码；再次执行 `git diff`。
4. [ ] 测试：正常新增、重复编号、借阅中删除、普通删除；记录响应和数据库变化。

## 收尾验收

- [ ] 两模块 CRUD 均完成。
- [ ] 每个 CRUD 至少一条 Network 和数据库证据。
- [ ] 两个自定义规则由后端拒绝证据证明。
